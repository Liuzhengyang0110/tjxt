# 项目待改进记录

本文档用于持续记录项目开发过程中发现、但暂不立即处理的问题，包括性能、可靠性、安全性、可维护性、代码规范和功能体验等方面。

## 记录约定

1. 每个问题使用唯一编号，格式为 `OPT-001`、`OPT-002`，后续依次递增。
2. 新问题需要同时添加到“事项汇总”和“详细记录”中。
3. 不删除已经完成或暂不处理的事项，通过更新状态保留处理历史。
4. 如果问题已经解决，需要补充完成日期、解决方案和关联提交或文件。
5. 一项记录只描述一个主要问题；关联度较高的子问题可以放在同一项中。

### 状态说明

| 状态 | 含义 |
| --- | --- |
| 待评估 | 已发现问题，尚未确认影响和处理方案 |
| 待处理 | 已确认需要改进，等待安排开发 |
| 处理中 | 正在实施改进 |
| 已完成 | 已完成修改并验证 |
| 暂不处理 | 当前决定不处理，需要说明原因 |

### 优先级说明

| 优先级 | 含义 |
| --- | --- |
| 高 | 影响核心功能、安全性或系统稳定性，应优先处理 |
| 中 | 对性能、可靠性或维护成本有明显影响，建议后续处理 |
| 低 | 影响较小，属于体验、规范或长期优化事项 |

## 事项汇总

| 编号 | 模块 | 问题概述 | 类型 | 优先级 | 状态 | 记录日期 |
| --- | --- | --- | --- | --- | --- | --- |
| OPT-001 | AI 对话 | 停止生成机制对 Redis 进行高频阻塞查询，且状态清理不完整 | 性能、可靠性 | 中 | 待处理 | 2026-08-26 |
| OPT-002 | 会话记忆 | Redis 会话枚举使用 `KEYS`，保存消息时全量删除并重写 | 性能、可靠性 | 中 | 待处理 | 2026-08-27 |

## 详细记录

### OPT-001：AI 对话停止生成机制待优化

**基本信息**

- 模块：AI 对话
- 类型：性能、可靠性
- 优先级：中
- 状态：待处理
- 记录日期：2026-08-26
- 涉及文件：`tj-aigc/src/main/java/com/tianji/aigc/service/impl/ChatServiceImpl.java`

**当前实现**

`ChatServiceImpl` 使用 Redis Hash 保存正在生成回答的 `sessionId`。开始生成时写入标记，停止生成时删除标记；大模型每返回一个响应分片，`takeWhile` 都会查询一次 Redis，判断对应标记是否仍然存在。

**发现的问题**

1. 每个响应分片都会执行一次 Redis 查询。一段回答可能产生大量分片，造成频繁的网络请求和额外的 Redis 压力。
2. `StringRedisTemplate` 是阻塞式客户端，在响应式流中高频调用可能阻塞处理线程，影响并发能力和流式响应速度。
3. 客户端直接断开连接时，当前 `doOnCancel` 只保存已生成内容，没有统一删除 Redis 状态，可能留下无效标记。
4. 服务异常退出时可能无法执行正常清理，Redis 中可能残留状态；当前标记没有过期或其他兜底清理机制。
5. 当前状态值固定为 `"true"`，实际只使用“存在或不存在”的语义，Redis Hash 的数据表达略显冗余。

**可能影响**

- AI 流式输出分片较多或并发量较大时，Redis 请求数量显著增加。
- 阻塞式 Redis 调用可能降低响应式处理链路的吞吐量。
- 无效标记可能导致状态判断不准确，并增加后续问题排查成本。

**后续优化方向**

1. 单实例场景可以使用本地线程安全集合或响应式取消信号，避免每个分片访问 Redis。
2. 分布式场景可以考虑使用 Redis Pub/Sub 传递停止事件，由收到事件的实例在本地取消对应生成任务。
3. 可以保存生成任务的 `Disposable`、`Sinks` 或其他取消句柄，直接取消响应流，不再轮询 Redis。
4. 使用 `doFinally` 统一处理正常完成、异常和取消后的状态清理。
5. 如果继续使用 Redis 标记，应增加过期时间或定期清理机制。
6. 如果只需要判断 `sessionId` 是否存在，可以评估使用 Redis Set；如果未来需要保存更多状态，则继续使用 Redis Hash，并定义明确的状态值。

**建议验收标准**

- 大模型每个响应分片不再触发一次阻塞式 Redis 查询。
- 正常完成、主动停止、客户端断开和异常场景均能正确清理生成状态。
- 多实例部署时，任意实例收到停止请求都能够停止对应生成任务。
- 完成并发和流式停止场景测试，不出现消息继续输出或状态残留。

**处理结果**

- 完成日期：待填写
- 解决方案：待填写
- 验证结果：待填写
- 关联提交：待填写

### OPT-002：Redis 会话记忆存取方式待优化

**基本信息**

- 模块：会话记忆
- 类型：性能、可靠性
- 优先级：中
- 状态：待处理
- 记录日期：2026-08-27
- 涉及文件：`tj-aigc/src/main/java/com/tianji/aigc/memory/RedisChatMemoryRepository.java`

**当前实现**

`findConversationIds()` 通过 Redis `KEYS CHAT:*` 查找所有会话；`saveAll()` 收到全量消息后，先删除原 Redis List，再逐条写入全部消息。

**发现的问题**

1. Redis `KEYS` 会遍历当前数据库中的全部 Key，数据量较大时可能阻塞 Redis，影响其他请求。
2. 每次保存消息都删除并重写全部历史记录，消息越多，序列化次数和 Redis 写入次数越多。
3. 删除旧列表和写入新列表不是原子操作，并发读取或服务异常时可能读取到空列表或不完整记录。

**后续优化方向**

1. 使用 `SCAN` 分批遍历会话 Key，或者额外维护会话 ID 集合，避免生产环境使用 `KEYS`。
2. 评估增量追加消息，避免每次重写全部历史记录。
3. 如果必须全量替换，可以使用事务、Lua 脚本或临时 Key 切换，保证更新过程的一致性。
4. 根据会话生命周期增加合理的过期和清理策略。

**建议验收标准**

- 查询会话 ID 时不再执行阻塞式 `KEYS`。
- 新增一条消息时，Redis 写入量不随已有历史消息数量线性增长。
- 并发读写和异常中断时，不出现聊天记录短暂为空或部分丢失。

**处理结果**

- 完成日期：待填写
- 解决方案：待填写
- 验证结果：待填写
- 关联提交：待填写

## 新增事项模板

后续发现待改进问题时，先在“事项汇总”中添加一行，再复制以下模板到“详细记录”末尾：

```markdown
### OPT-XXX：问题标题

**基本信息**

- 模块：
- 类型：性能 / 可靠性 / 安全性 / 可维护性 / 代码规范 / 功能体验
- 优先级：高 / 中 / 低
- 状态：待评估 / 待处理 / 处理中 / 已完成 / 暂不处理
- 记录日期：YYYY-MM-DD
- 涉及文件：

**当前实现**

简要说明相关代码和当前处理方式。

**发现的问题**

1. 问题描述。

**可能影响**

- 说明对功能、性能、稳定性或维护成本的影响。

**后续优化方向**

1. 可考虑的解决方案。

**建议验收标准**

- 可以验证改进是否完成的具体标准。

**处理结果**

- 完成日期：待填写
- 解决方案：待填写
- 验证结果：待填写
- 关联提交：待填写
```
